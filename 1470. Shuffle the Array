class Solution:
    def shuffle(self, nums: List[int], n: int) -> List[int]:
        s = []
        p1 = 0
        p2 = n
        while p2 <= len(nums)-1:
            s.append(nums[p1])
            s.append(nums[p2])
            p1 += 1
            p2 += 1
        return s
        
